
# Mifer 使用教程 — 从零开始，人人都能看懂

---

## 什么是 Mifer？

简单来说，**Mifer 就是一个运行在你电脑上的 AI 助手程序**。你可以像聊天一样告诉它你想做什么，它会帮你读文件、写文件、搜索网页、查资料、执行命令等等。

打个比方：如果说 ChatGPT 是一个在浏览器里聊天的 AI，那 Mifer 就是一个**直接在你电脑上干活的 AI**——它能真正操作你电脑上的文件和程序。

> 如果你是第一次接触这类工具，别担心，本教程会一步一步带你上手。

---

## 第一步：我需要准备什么？

### 硬件要求

| 项目 | 最低要求 | 说明 |
|------|----------|------|
| 操作系统 | Windows 10 或更高版本 | 大部分人的电脑都满足 |
| 内存 | 4GB 以上 | 程序本身占用很小 |
| 硬盘空间 | 100MB 以上 | 程序+配置文件 |
| 网络 | 需要联网 | AI 需要连接互联网才能工作 |

> **怎么查看自己的系统信息？**  
> 在键盘上按 `Win + Pause`（Windows 键 + 暂停键），就能看到你的系统版本和内存大小。

### 你必须有的东西：API Key（AI 服务密钥）

Mifer 本身不包含 AI 大脑，它需要连接到一个 AI 服务商才能"思考"。这就需要你有一个 **API Key**（可以理解为"AI 服务的账号密码"）。

> **通俗理解**：API Key 就像你家的门钥匙，有了它 AI 服务商才让你使用他们的 AI 模型。这个密钥是一串英文字母和数字组成的字符串，比如 `sk-abc123def456...`。

#### 推荐：使用 DeepSeek（国内用户首选，便宜好用）

1. 打开浏览器，访问 **https://platform.deepseek.com/**
2. 点击右上角「注册」，用手机号注册一个账号
3. 注册完成后登录，点击左侧菜单的「API Keys」
4. 点击「创建 API Key」，给它起个名字（比如"我的Mifer"）
5. **立即复制**生成的密钥（它只会显示一次！）
6. 把这个密钥保存到一个安全的地方，后面要用

> **费用说明**：DeepSeek 采用按量计费，新用户通常有免费额度。正常使用一个月几块钱就够了，比一杯奶茶还便宜。

#### 备选方案

| 服务商 | 网址 | 特点 |
|--------|------|------|
| 阿里云百炼 | https://bailian.console.aliyun.com/ | 国产，通义千问系列模型 |
| OpenAI | https://platform.openai.com/ | ChatGPT 官方，需要国外信用卡 |
| Claude | https://console.anthropic.com/ | Anthropic 官方，需要国外手机号 |

> **新手建议**：国内用户选 **DeepSeek** 最简单，注册方便，价格便宜，支持支付宝充值。

---

## 第二步：下载和安装

### 下载程序

1. 打开浏览器，访问 Mifer 的下载页面（问给你这个程序的人要下载地址）
2. 根据你的电脑系统选择正确的版本：

| 你的电脑 | 选择这个文件 |
|----------|-------------|
| Windows 64位（大多数电脑） | `mifer-vX.X.X-windows-amd64.zip` |
| Windows ARM（Surface 等） | `mifer-vX.X.X-windows-arm64.zip` |

> **怎么知道自己的 Windows 是 64 位还是 ARM？**  
> 按 `Win + I` 打开设置 → 点击「系统」→ 点击「关于」→ 看「系统类型」一栏。显示"64 位操作系统"就选 amd64。

### 安装（其实不需要安装）

Mifer 是"绿色软件"，不需要像普通软件那样安装。你只需要：

1. 下载压缩包（`.zip` 文件）
2. 在你想要的位置新建一个文件夹，比如 `D:\mifer`
3. 把压缩包里的 `mifer.exe` 解压到这个文件夹里
4. 双击 `mifer.exe` 就能运行了！

> **第一次运行注意**：Windows 可能会弹出"Windows 保护了你的电脑"的蓝色警告框。这是因为程序没有微软的数字签名。点击「更多信息」，然后点击「仍要运行」即可。

---

## 第三步：配置 API Key（让 AI 能工作）

第一次运行 Mifer 时，它会自动在程序所在目录的 `config` 文件夹下创建一个名为 `dev.yaml` 的配置文件。

### 方法一：通过配置文件设置（推荐）

1. 双击运行 `mifer.exe` 一次，然后关掉它
2. 这时程序目录下会多出一个 `config` 文件夹，里面有 `dev.yaml` 文件
3. 用记事本打开 `dev.yaml`（右键文件 → 打开方式 → 记事本）
4. 找到这几行：

```yaml
ai:
  backends:
    default:
      provider: "openai"
      base_url: "https://api.deepseek.com"
      model: "deepseek-v4-flash"
      api_key: ""           # ← 在这里填入你的 API Key
```

5. 在 `api_key: ""` 的引号里填入你的密钥，比如：

```yaml
      api_key: "sk-abc123def456ghi789jkl"
```

6. 保存文件（`Ctrl + S`），关掉记事本
7. 重新运行 `mifer.exe`，搞定！

### 方法二：通过环境变量设置（Windows 系统）

如果你不想改配置文件，也可以用系统环境变量，这样更安全：

1. 在桌面或文件夹中，**按住 Shift 键**，然后在空白处右键，选择「在此处打开 PowerShell 窗口」
2. 输入以下命令（把 `sk-你的密钥` 替换成你实际的密钥）：

```powershell
$env:MIFER_AI_DEFAULT_APIKEY="sk-你的密钥"
.\mifer.exe
```

> 这个方法的缺点是每次打开新窗口都要重新设置。如果想让环境变量永久生效，可以看附录中的"设置永久环境变量"。

---

## 第四步：开始使用

### 基本操作

运行 `mifer.exe` 后，你会看到一个终端窗口，里面有一些绿色的文字和状态信息。这就是 Mifer 的命令行界面。

#### 最重要的操作：直接打字聊天

在输入框中**直接输入你想说的话**，然后按 **Enter（回车键）**，AI 就会开始回答你。

比如你可以试试输入：
- "你好，请介绍一下你自己"
- "帮我分析一下这个文件夹里有什么文件"

#### 常用命令

在输入框中输入以 `/` 开头的内容，就是给 Mifer 下命令：

| 命令 | 做什么 | 什么时候用 |
|------|--------|-----------|
| `help` | 显示所有可用命令 | 忘记命令的时候 |
| `exit` 或 `quit` | 退出程序 | 用完了想关掉 |
| `/viewmemory` | 查看当前对话记录 | 想看之前聊了什么 |
| `/clear` | 开始一段新对话 | 想换个话题重新聊 |
| `/reback` | 回退到之前的某次对话 | AI 改错了想撤销 |
| `/prompt` | 查看或修改 AI 的"人设" | 想让 AI 换种风格说话 |
| `/reload` | 重新加载配置 | 修改了配置文件后 |
| `/config` | 打开配置文件 | 想修改设置 |
| `/compact` | 压缩对话上下文 | 聊天太长了节省资源 |
| `/agents` | 查看可用的子 AI | 想看有哪些 AI 在干活 |
| `/mcp` | 查看扩展工具状态 | 想确认外部工具是否连接 |
| `/skill` | 查看加载的技能列表 | 想看有哪些技能可用 |
| `/plan` | 查看项目计划文件 | 想看 AI 做的项目计划 |

### 界面说明

当你运行 Mifer 后，终端界面分为两个区域：

- **左边（主区域）**：显示你和 AI 的对话内容。你说的和 AI 回答的都在这里。
- **右边（侧边栏）**：显示 AI 内部的工作状态。比如它正在调用什么工具、哪个子 AI 在干活。

#### 一些提示

- AI 回答时，你会在左下角看到一个旋转的动画，表示它正在"思考"
- 如果 AI 想做某些操作（比如写文件），会弹出一个确认框询问你是否允许
- 按 `Tab` 键可以自动补全命令
- 按 `↑`（上箭头键）和 `↓`（下箭头键）可以翻看之前输入过的内容
- 按 `Ctrl + C` 可以随时退出程序
- 界面太小导致文字显示不全时，把终端窗口**拉大一点**即可

---

## 第五步：了解 Mifer 能做什么

### Mifer 有几个"分身"（子 Agent）

Mifer 内部有多个专门负责不同任务的"分身"（专业术语叫"Agent"），它们各司其职：

| 分身名称 | 负责什么 | 比喻 |
|----------|----------|------|
| **Mifer**（总指挥） | 理解你的需求，调度其他分身 | 就像项目经理 |
| **MiEditer**（编辑） | 读文件、写文件、创建文件、看图 | 就像文书 |
| **MiCommander**（命令） | 在电脑上执行终端命令 | 就像运维 |
| **MiPlanner**（规划） | 制定项目计划 | 就像设计师 |
| **MiAuditor**（审计） | 检查代码安全性 | 就像保安 |
| **MiSummarizer**（总结） | 读文档、做总结 | 就像秘书 |
| **MiQQ**（QQ助手） | QQ 聊天中回答问题 | 就像客服 |

> 你不用手动选择用哪个分身——Mifer 会自动判断该派谁去干活。

### Mifer 能用的工具

| 工具 | 能做什么 |
|------|----------|
| 文件读取 | 读你电脑上的文件内容 |
| 文件写入 | 修改或覆盖文件 |
| 文件创建 | 创建新文件 |
| 文件查看 | 查看文件（包括图片） |
| 图片生成 | 用 AI 生成图片 |
| 命令执行 | 在你的电脑上运行命令行 |
| 知识库搜索 | 从你的文档库里搜索相关内容 |
| 知识库存储 | 把文档存入知识库 |
| 网页搜索 | 在互联网上搜索信息 |
| 网页抓取 | 读取指定网页的内容 |
| QQ 消息发送 | 通过 QQ 发送私聊或群聊消息 |

### 安全提示

Mifer 在以下情况会先问你，不会自作主张：
- 写文件或修改文件时
- 执行命令行操作时

你会看到一个确认弹窗，选择「允许」或「拒绝」。这是为了防止 AI 误操作，建议保持开启。

---

## 常见问题（遇到问题先看这里）

### Q：运行后提示"加载配置失败"？
**A：** 检查一下你运行 mifer.exe 的文件夹有没有写入权限。确保程序所在的文件夹不是只读的。如果程序在系统盘（C 盘），建议移到 D 盘或其他盘。

### Q：AI 不回复，一直在转圈？
**A：** 最常见的原因是 API Key 没设置对。请检查：
1. 确认 `api_key` 已经填入了正确的密钥（注意不要有多余的空格或引号）
2. 确认 `base_url` 地址正确（DeepSeek 的是 `https://api.deepseek.com`）
3. 确认你的 API Key 账户里还有余额
4. 查看同目录下的 `debug.log` 文件，里面可能有更详细的错误信息

### Q：提示"网络连接失败"？
**A：** 检查一下：
1. 你的电脑是否连上了网络（打开浏览器试试能不能上网）
2. 公司或学校网络可能有防火墙，尝试用手机热点测试一下
3. API 地址是否被墙（国内访问 OpenAI 等国外服务可能需要特殊网络环境）

### Q：怎么换成别的 AI 模型？
**A：** 在 `dev.yaml` 配置文件中修改 `model` 那一行：

```yaml
# 比如从 DeepSeek 换成通义千问：
default:
  provider: "openai"
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  model: "qwen-plus"          # 改这里
  api_key: "sk-你的阿里云密钥"
```

> `provider` 填 `"openai"` 是因为大多数国产 AI 都兼容 OpenAI 的接口格式，不是只能连 OpenAI。

### Q：对话太长了，AI 开始变慢或遗忘前面的内容？
**A：** 这是正常的。你可以：
- 输入 `/compact` 让 AI 自动压缩之前的对话重点
- 输入 `/clear` 开启一段全新的对话
- 对话上下文默认超过设定阈值时，AI 会自动压缩，你不需要手动操作

### Q：程序被杀毒软件拦截了？
**A：** Go 语言编译的程序有时会被误报。你可以：
1. 在杀毒软件中将 `mifer.exe` 添加为信任程序
2. 或将 mifer 文件夹添加为排除目录

### Q：怎么更新到最新版本？
**A：** 下载新版本的压缩包，把里面的 `mifer.exe` 替换掉旧版本即可。配置文件不会受影响。

---

## 进阶功能（可选，想了解更多再看）

### 知识库功能（让 AI 读你的文档）

Mifer 支持"知识库检索增强生成"（简称 RAG），意思是你可以把文档喂给 AI，之后对话时 AI 能自动从这些文档里找相关内容来回答。

#### 需要额外安装（Docker）

知识库功能需要两个后台服务：

| 服务 | 作用 | 比喻 |
|------|------|------|
| Qdrant | 存储文档内容（向量数据库） | 就像图书馆的书架 |
| Ollama | 把文档转成 AI 能理解的格式 | 就像翻译官 |
| NapCatQQ | QQ 协议桥接（登录 QQ 收发消息） | 就像电话转接器 |

#### 安装步骤（需要先安装 Docker Desktop）

1. 从 https://www.docker.com/products/docker-desktop/ 下载并安装 Docker Desktop
2. 安装完成后重启电脑
3. 在 Mifer 所在目录打开 PowerShell
4. 运行：`docker-compose up -d`
5. 等待下载完成（第一次会比较慢，需要下载几个 GB 的文件）
6. 拉取嵌入模型：`docker-compose exec ollama ollama pull nomic-embed-text`

全部完成后，你就可以对 AI 说"帮我把这个文档存到知识库"来使用知识库功能了。

> **速度说明**：知识库功能需要下载 Ollama 模型，文件较大（约 300MB），请耐心等待。之后每次启动 Docker 服务只需要几秒钟。

### 网页搜索功能

Mifer 内置了网页搜索工具。默认使用 SearXNG（一个免费的聚合搜索引擎）。

启用步骤：
1. 确保 Docker Desktop 正在运行
2. 运行 `docker-compose up -d searxng`
3. 之后 AI 就能自动上网搜索了

如果你不想安装 Docker，也可以在配置文件中设置 `search.provider: "duckduckgo"`，使用免费的 DuckDuckGo 搜索（不需要额外安装任何东西，但可能被限速）。

### 自定义 Agent（创建你自己的 AI 分身）

在配置文件的 `agents:` 部分，你可以添加新的 AI 分身：

```yaml
agents:
  - name: "我的翻译官"
    description: "专门做翻译的助手"
    instruction: "你是一个专业的翻译，把所有中文翻译成英文。"
    model: "sonnet"
    tools:
      - file_reader
      - file_writer
```

### MCP 扩展工具（连接外部工具）

MCP（Model Context Protocol）是 AI 工具扩展协议，可以给 Mifer 接入第三方工具，比如：
- GitHub 仓库管理
- 数据库查询
- 文件系统操作

在配置文件的 `mcp.servers:` 部分配置。通常需要一定的技术背景，新手可以暂时跳过。

### 运行模式详解

| 命令 | 效果 | 适用场景 |
|------|------|----------|
| `.\mifer.exe` | 同时启动服务+对话界面 | 日常使用 |
| `.\mifer.exe serve` | 只启动后台服务 | 当服务器用 |
| `.\mifer.exe chat` | 只启动对话界面 | 连接已有的服务 |
| `.\mifer.exe chat --abc123` | 指定会话 ID 进入对话 | 恢复历史会话 |

### 文件快照

Mifer 默认开启了文件快照功能，在每轮对话后自动记录文件状态。**智能优化**：仅在文件实际被修改时才保存新快照，纯聊天的对话不会产生多余快照文件。回退时自动查找最近的可用快照，不需要快照轮次与对话轮次精确对应。

如果你让 AI 改了文件之后后悔了，可以用 `/reback` 命令回退到修改前的状态。**注意**：如果在同一项目目录下同时打开了多个 Mifer 窗口并各自回退，可能会互相覆盖文件。推荐一个项目目录同时只保留一个活跃对话。

### QQ Bot（QQ 机器人）

Mifer 支持作为 QQ 机器人运行，接收 QQ 私聊和群聊消息并自动回复。

**启用步骤**：
1. 运行 `docker-compose up -d napcat` 启动 NapCatQQ（QQ 协议桥）
2. 浏览器打开 `http://localhost:6099`，扫码登录你的 QQ 号
3. 在 NapCat WebUI 中：网络配置 → 新建 WebSocket 服务端 → 主机 `0.0.0.0` 端口 `3001` → 启用
4. 在 Mifer 配置文件（`dev.yaml`）中设置：
   ```yaml
   qq:
     enabled: true
     bot:
       qq: 你的QQ号
   ```
5. 启动 Mifer：`.\mifer.exe`

**功能说明**：
- 私聊：直接 @ Bot 或在私聊窗口发送消息即可对话
- 群聊：需要在消息中 @ Bot 才会回复（`mention_only` 模式）
- 每个用户的记忆是独立的，Bot 会记住你们之前的聊天内容
- QQ 通道使用专用的轻量 AI（MiQQ），只做纯文本回复，不会尝试操作文件或执行命令

---

## 附录

### 名词解释

| 术语 | 通俗解释 |
|------|----------|
| AI / 大模型 | 能理解和生成文字、图片的程序，就像"数字大脑" |
| API Key | 访问 AI 服务的密码，一串字母数字组成的密钥 |
| Agent（智能体） | Mifer 内部的"分身"，每个分身负责不同任务 |
| Token | AI 处理文本的计量单位，大致等于 1-2 个汉字 |
| RAG（检索增强） | 让 AI 在回答前先查你的文档，回答更准确 |
| MCP | AI 工具扩展协议，用来连接外部工具 |
| SSE（流式输出） | AI 一个字一个字地往外蹦，而不是等全部写完再显示 |
| CLI / TUI | 命令行界面 / 终端用户界面（就是那个黑底绿字的窗口） |
| Docker | 一个让程序在"容器"里运行的工具，用于安装 Qdrant 等辅助服务 |
| YAML | 一种配置文件格式，用缩进表示层级关系 |

### 配置文件夹在哪？

| 运行模式 | 配置文件位置 | 对话记录位置 |
|----------|-------------|-------------|
| 开发模式（默认） | `程序目录\config\dev.yaml` | `程序目录\memory\` |
| 生产模式 | `C:\Users\你的用户名\.mifer\config\prod.yaml` | `C:\Users\你的用户名\.mifer\memory\` |

### 设置永久环境变量（Windows）

如果你不想每次都在命令行里输入 API Key，可以把环境变量设置成永久的：

1. 按 `Win + R`，输入 `sysdm.cpl`，回车
2. 点击「高级」选项卡 → 「环境变量」
3. 在「用户变量」区域点击「新建」
4. 变量名填：`MIFER_AI_DEFAULT_APIKEY`
5. 变量值填：你的 API Key（如 `sk-abc123...`）
6. 点击「确定」保存
7. 重新打开终端窗口，环境变量就生效了

---

> 如果本教程没有解决你的问题，请联系软件的提供者，或查看项目文档中的更多信息。
>
> — Mifering...
